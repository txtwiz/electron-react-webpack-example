import React from 'react';
import Info from './Info'; // eslint-disable-line no-unused-vars
import Button from './Button'; // eslint-disable-line no-unused-vars


class App extends React.Component {

  render() {
    return (
      <div>
        <Info/>
        <Button/>
      </div>
    );
  }
}

export default App;
